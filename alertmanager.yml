global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'alerts@jpmorgan-api.com'
  smtp_auth_username: 'your-email@gmail.com'
  smtp_auth_password: 'your-app-password'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'email-alerts'
  routes:
  - match:
      alertname: JPMorganAPIHighErrorRate
    receiver: 'email-alerts'
  - match:
      alertname: JPMorganAPIDown
    receiver: 'email-alerts'

receivers:
- name: 'email-alerts'
  email_configs:
  - to: 'admin@equityshieldadvocates.com'
    send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
